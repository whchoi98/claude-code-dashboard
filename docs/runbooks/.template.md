# Runbook: <short incident name>

## Symptom

What the on-call sees first. Include alert name, dashboard screenshot description, or exact error text.

## Impact

Who is affected, how visibly. (e.g., "Dashboard returns 504 for all users" or "Cost page empty but other pages fine").

## Diagnose

Stepwise commands to confirm the symptom and narrow the cause.

```bash
# Example
aws elbv2 describe-target-health --target-group-arn …
```

## Mitigate

Fast actions to restore service (manual listener recreation, stack redeploy, flag flip). Each step should be copy-pasteable.

```bash
aws elbv2 create-listener …
```

## Root cause analysis

Once service is restored, fill in:

- **Trigger**: what started it.
- **Why it wasn't caught**: gap in monitoring/tests/review.
- **Durable fix**: PR link, ADR reference, or follow-up issue.

## Related

- ADR link
- Previous incidents
