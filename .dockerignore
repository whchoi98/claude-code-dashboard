node_modules
dist
.env
.env.*
.git
.github
infra
collector
*.log
README.md
docs
screenshots
sample
tests
_local
.harness-eval
# Note: CHANGELOG.md is intentionally NOT excluded — Vite's `?raw` import
# bundles its content into the SPA at build time so the /changelog page
# can render it without a runtime fetch. See src/pages/Changelog.tsx.
CLAUDE.md
